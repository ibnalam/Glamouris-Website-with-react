import React from 'react'

const Sectionlargeheading = ({className,text}) => {
  return (
    <h2 className={`font-poppins font-bold pt-[24px] text-[32px] ${className}`}>{text}</h2>
  )
}

export default Sectionlargeheading